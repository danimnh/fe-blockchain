version: '3.9'

services:
  bm-frontend:
    image: danimnh/bm-bc-pwa
    container_name: bm-frontend
    restart: unless-stopped
    ports:
      - "${PORT}:3000"
    environment:
      PORT: ${PORT}
      REACT_APP_BC_API_URL: ${REACT_APP_BC_API_URL}
